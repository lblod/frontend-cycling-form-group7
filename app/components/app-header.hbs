<AuMainHeader
  @brandLink="https://www.vlaanderen.be/nl"
  @homeRoute=""
  @appTitle="Cycling form group 7"
  @contactRoute=""
>
  {{#if this.session.isAuthenticated}}
    {{! TODO: Add some logic showing actual username or sth }}
    Municipality name
  {{else}}
    <AuLink
      @route="login"
      @skin={{if @isCompact "primary" "button"}}
      @icon="login"
    >
      Meld je aan
    </AuLink>
  {{/if}}
</AuMainHeader>